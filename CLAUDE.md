# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Environment

- Python 3.13.2
- Virtual environment at `.venv/` — activate with `.venv/Scripts/activate` (Windows)
- Run commands using `.venv/Scripts/python` or activate the venv first

## Project

**shopmind** is a new Python project. No source files exist yet.

When the project takes shape, update this file with:
- How to install dependencies (`pip install -r requirements.txt` or `pip install -e .`)
- How to run the application
- How to run tests (e.g., `pytest` or `python -m pytest tests/`)
- Any linting/formatting commands
- High-level architecture notes

## Requirements
Never add a co-author to the git commit.